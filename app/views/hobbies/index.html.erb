<%= button_to "Log Out", destroy_user_session_path, method: :delete %>

<h1>Welcome, to hobbybook!</h1>
<br />
<h2> Your hobbies </h2>
<ul>
  <% @hobbies. each do |h|%>
    <li>
      <%= h.name %> - <%= h.description %>
    </li>
  <% end %>
</ul>

<%= link_to "New Hobby", new_hobby_path, class: "btn blue" %>